interface:
  display_name: "Memory Init"
  short_description: "Scaffold reusable project memory files"
  default_prompt: "Use $memory-init to initialize project memory files and add AGENTS.md guidance for future agents."
