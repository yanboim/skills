interface:
  display_name: "GitLab MR Inline Comment"
  short_description: "Post one anchored inline comment on a GitLab merge request"
  default_prompt: "Use $gitlab-mr-inline-comment to post a single GitLab merge request inline comment on a specific file and new-side line, reusing the current glab authentication and failing explicitly when the anchor is invalid."
